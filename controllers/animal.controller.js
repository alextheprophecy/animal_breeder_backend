const {queryProteus} = require("./stablediffusion.controller");
const {queryChatGPT} = require("./chatgpt.controller");
const query = "A realistic sideview picture of an imaginary creature with the head of an eagle and the body of a tiger, set against a plain, empty background. The eagle's head features its sharp beak, piercing eyes, and majestic feathers, seamlessly blending into the powerful, striped body of a tiger, complete with muscular legs and a long tail. The creature is depicted in a dynamic pose, showcasing the unique combination of its form."

const getChild = (parent1, parent2) => {

}


module.exports = {
    getChild
}